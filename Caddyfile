0.0.0.0:5000 {
    root /var/www/html
    tls off
    gzip
    log stdout
    errors stdout
    rewrite {
        if {file} not favicon.ico
        if {file} not favicon.png
        to {path} {path}/ /index.html
    }
}
